//write a program to take a string as an input from the user using %c and %s ,
// confirms that the strings are equal;
 #include<stdio.h>

int main(){
    char st[16];
    printf("enter the string :");
    scanf("%s",st);

     printf("%s",st);
    
    return 0;
}