#include<stdio.h>
//write a program to find the area of circle 
int main(){
    int r ;// radous of circle
    float pi=3.14;
    printf("enter radious of the circle :");
    scanf("%d",&r);
    printf("area of the circle is %f",pi*r*r);
    
    return 0;
}