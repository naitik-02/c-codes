// puts and gets in c
#include <stdio.h>

int main()
{
    FILE *ptr;
    ptr = fopen("demo.txt", "w");
    // printf("the value of my character is %c \n", fgetc(ptr));
    // printf("the value of my character is %c \n", fgetc(ptr));
    // printf("the value of my character is %c \n", fgetc(ptr));
    // printf("the value of my character is %c \n", fgetc(ptr));
    // printf("the value of my character is %c \n", fgetc(ptr));
    // printf("the value of my character is %c \n", fgetc(ptr));
    // printf("the value of my character is %c \n", fgetc(ptr));
    // printf("the value of my character is %c \n", fgetc(ptr));
    // printf("the value of my character is %c \n", fgetc(ptr));
    // printf("the value of my character is %c \n", fgetc(ptr));


//puts 


// putc('c',ptr);
// putc('a',ptr);
// putc('r',ptr);
// putc('t',ptr);
// putc('o',ptr);
// putc('o',ptr);
// putc('n',ptr);
// putc('s',ptr);
// fclose(ptr);

    return 0;
}